{
    "title": "CSS Foundations",
    "has_project": false,
    "video_url": "https://www.youtube.com/watch?v=1PnVor36_40",
    "content": "# 🎨 CSS Foundations & The Box Model\n\nWhile HTML provides the skeleton of your application, **CSS (Cascading Style Sheets)** provides the skin, clothing, and makeup. It dictates exactly how elements are painted onto the browser screen.\n\nHowever, CSS is often deeply misunderstood by beginners, leading to \"CSS spaghetti\" where developers randomly add `margin-top: -50px` until things look right on their specific screen. Professional engineers understand the mathematical algorithms behind CSS rendering.\n\n---\n\n## 1️⃣ Anatomy of a CSS Rule\n\nA CSS rule dictates *what* element to target and *how* to change it.\n\n```css\n/* The Selector: Targets all <button> elements */\nbutton {\n  /* Declaration Block starts */\n  \n  /* Property: Value pair */\n  background-color: blue;\n  color: white; \n  border-radius: 4px;\n  \n  /* Declaration Block ends */\n}\n```\n\n### The Three Ways to Apply CSS\n\n1. **Inline CSS (Worst Practice)**: Writing CSS directly inside the HTML tag. Inherently impossible to reuse and notoriously difficult to override.\n   `<h1 style=\"color: red;\">Header</h1>`\n\n2. **Internal CSS (Bad Practice)**: Placing a `<style>` tag inside the `<head>` of a specific HTML document. Only styles that specific page.\n\n3. **External CSS (Best Practice)**: Writing styles in a separate `.css` file and linking it. This allows the browser to cache the file, making the website load exponentially faster on subsequent pages.\n   `<link rel=\"stylesheet\" href=\"styles.css\">`\n\n---\n\n## 2️⃣ The Cascade and Specificity algorithms\n\nWhy is it called *Cascading* Style Sheets? Because rules cascade downwards. If two identical rules target the same element, the one written *last* wins.\n\n```css\np { color: red; }\np { color: blue; } /* All paragraphs will be blue */\n```\n\n### Specificity: The Battle for Dominance\nWhat if the rules aren't identical? CSS calculates a **Specificity Score** based on what type of selector you use.\n\n| Selector Type | Example | Points | Dominance |\n|---|---|---|---|\n| **Universal tag** | `* { margin: 0; }` | 0 | Lowest |\n| **Element tag** | `button { color: red; }` | 1 | Very Low |\n| **Class selector** | `.btn-primary { color: blue; }` | 10 | High (Standard way to style) |\n| **ID selector** | `#submit-btn { color: green; }` | 100 | Very High (Avoid using IDs for styling) |\n| **Inline Style** | `<button style=\"color: purple;\">` | 1000 | Absolute |\n| **!important** | `color: black !important;` | Infinity| Nuclear Option (Never use this unless overriding a 3rd party library) |\n\n**Example Calculation:**\n```css\n/* Score: 10 (Class) + 1 (Element) = 11 */\n.container button { background: red; }\n\n/* Score: 10 (Class) = 10 */\n.btn { background: blue; }\n```\nEven if `.btn` is written *after* `.container button` in the file, the specific button inside `.container` will still be red, because 11 beats 10 mathematically.\n\n---\n\n## 3️⃣ The Holy Grail: The CSS Box Model\n\nEvery single element in HTML is a rectangular box. It does not matter if it looks like a circle (`border-radius: 50%`) or a triangle; mathematically, the browser calculates it as a rectangle.\n\nThe Box consists of four layers, moving from the inside out:\n\n```mermaid\ngraph TD\n    subgraph \"Margin (Invisible outer spacing pushing other elements away)\"\n        subgraph \"Border (The visible wall enclosing the padding and content)\"\n            subgraph \"Padding (Invisible inner spacing pushing content inward)\"\n                C[Content (Text, Images, Child Elements)]\n            end\n        end\n    end\n```\n\n### 📦 Example Code\n\n```css\n.card {\n  width: 300px;\n  padding: 20px;\n  border: 5px solid black;\n  margin: 50px;\n}\n```\n\n### The `box-sizing: border-box` Hack\nBy default, padding and borders are *added* to the width of the element.\nIn the example above, the `.card` is actually `300px + 20px (left pad) + 20px (right pad) + 5px (left border) + 5px (right border) = 350px wide`. This makes responsive math incredibly frustrating.\n\n**The Golden Reset:** Every professional project starts with this snippet to force the padding and border to push *inward* instead of expanding *outward*.\n\n```css\n*, *::before, *::after {\n  box-sizing: border-box;\n  margin: 0;\n  padding: 0;\n}\n```\nNow, if you declare `width: 300px`, the card is exactly 300 pixels wide on the screen.\n\n---\n\n## 4️⃣ Display Properties\n\nThe `display` property dictates how the box interacts with the boxes around it in the normal document flow.\n\n### `display: block`\n- Elements (like `<div>`, `<h1>`, `<p>`) take up 100% of the available width of their parent container.\n- They aggressively force a line break, pushing anything after them onto the next line.\n- You can set `width`, `height`, `margin`, and `padding` freely.\n\n### `display: inline`\n- Elements (like `<span>`, `<a>`, `<strong>`) only take up as much width as their inner content absolutely needs.\n- They sit side-by-side with other inline elements, flowing like words in a paragraph.\n- **Critical Limitation**: You *cannot* set `width`, `height`, `margin-top`, or `margin-bottom` on an `inline` element. The browser ignores those instructions.\n\n### `display: inline-block`\n- A hybrid. It sits side-by-side like an `inline` element, but the browser respects `width`, `height`, and vertical `margin` properties like a `block` element. (Perfect for buttons).\n\n### `display: none`\n- Mathematically rips the element out of the Document Object Model. It essentially ceases to exist. Screen readers ignore it, and other elements collapse into the space it used to occupy. (Contrast with `visibility: hidden`, which turns the element invisible but leaves an empty ghost gap where it used to be).\n\n---\n\n## 5️⃣ Colors and Units\n\n### Professional Units of Measurement\nNever use hardcoded `px` (pixels) for typography. If a visually impaired user increases their browser's default font size from 16px to 24px, a hardcoded `font-size: 14px` will ignore their settings and remain tiny, violating accessibility laws.\n\n| Unit | What it is | Use Case |\n|---|---|---|\n| `px` | Absolute pixels mapping to screen resolution. | Precise borders (`border: 1px solid`). |\n| `rem` | \"Root em\". Relative to the `<html>` root font size (default 16px). | **Typography, Padding, Margins.** (`1rem = 16px, 1.5rem = 24px`). |\n| `%` | Percentage relative to the parent container. | Creating fluid columns (`width: 50%`). |\n| `vw` / `vh` | Viewport Width / Viewport Height (0 - 100). | Creating massive heroes taking the entire screen (`height: 100vh`). |\n\n### Color Formats\n- **Hex**: `#FF0000` (Red). Standard but inflexible.\n- **RGB / RGBA**: `rgba(255, 0, 0, 0.5)` (Red with 50% opacity/transparency).\n- **HSL**: `hsl(0, 100%, 50%)` (Hue, Saturation, Lightness). The professional choice, because creating a \"darker\" version of a color just requires reducing the Lightness percentage.\n\n---\n\n## 💡 Summary Checklist for CSS\n- [ ] Memorize the Cascade and Specificity calculation (IDs beat Classes, Classes beat Tags).\n- [ ] Understand the Box Model from inside out (Content → Padding → Border → Margin).\n- [ ] Add the `box-sizing: border-box` reset to every project.\n- [ ] Stop using `px` for fonts. Use `rem` for accessibility.\n- [ ] Understand the fundamental difference between `block` and `inline` elements.\n",
    "questions": [
        {
            "question_text": "What does CSS stand for?",
            "options": [
                "Creative Style Sheets",
                "Cascading Style Sheets",
                "Computer Style Sheets",
                "Colorful Style Sheets"
            ],
            "correct_answer": 1,
            "explanation": "Cascading refers to the way styles are applied based on priority and order."
        },
        {
            "question_text": "Which property is used to change the background color of an element?",
            "options": [
                "color",
                "bgcolor",
                "background-color",
                "fill"
            ],
            "correct_answer": 2,
            "explanation": "background-color is the standard CSS property for setting background fills."
        },
        {
            "question_text": "What are the four parts of the CSS Box Model (from inside to outside)?",
            "options": [
                "Content, Margin, Padding, Border",
                "Content, Padding, Border, Margin",
                "Padding, Content, Border, Margin",
                "Margin, Border, Padding, Content"
            ],
            "correct_answer": 1,
            "explanation": "The order is: Content -> Padding (space inside) -> Border -> Margin (space outside)."
        },
        {
            "question_text": "Which CSS selector has the highest specificity?",
            "options": [
                "Class selector (.my-class)",
                "ID selector (#my-id)",
                "Element selector (div)",
                "Universal selector (*)"
            ],
            "correct_answer": 1,
            "explanation": "ID selectors are more specific than classes, which are more specific than elements."
        },
        {
            "question_text": "How do you select an element with id 'header' in CSS?",
            "options": [
                ".header",
                "#header",
                "*header",
                "header"
            ],
            "correct_answer": 1,
            "explanation": "The '#' symbol is used for IDs in CSS."
        },
        {
            "question_text": "What does the 'position: absolute' property do?",
            "options": [
                "Positions the element relative to its normal position",
                "Positions the element relative to its nearest positioned ancestor",
                "Fixed the element to the viewport",
                "Centers the element automatically"
            ],
            "correct_answer": 1,
            "explanation": "If no positioned ancestor exists, it uses the document body."
        },
        {
            "question_text": "Which property is used to change the text color of an element?",
            "options": [
                "text-color",
                "fgcolor",
                "color",
                "font-color"
            ],
            "correct_answer": 2,
            "explanation": "The 'color' property specifically targets the foreground text color."
        },
        {
            "question_text": "What is the difference between 'padding' and 'margin'?",
            "options": [
                "Padding is outside the border; Margin is inside",
                "Padding is inside the border; Margin is outside",
                "They are exactly the same",
                "Padding handles text size; Margin handles spacing"
            ],
            "correct_answer": 1,
            "explanation": "Padding adds space between content and border; Margin adds space between elements."
        },
        {
            "question_text": "How do you make the text bold in CSS?",
            "options": [
                "font-style: bold",
                "text-decoration: bold",
                "font-weight: bold",
                "style: bold"
            ],
            "correct_answer": 2,
            "explanation": "font-weight controls the thickness of the characters."
        },
        {
            "question_text": "What does 'z-index' control?",
            "options": [
                "The zoom level of the page",
                "The horizontal position of an element",
                "The stack order (layering) of positioned elements",
                "The size of the border"
            ],
            "correct_answer": 2,
            "explanation": "Higher z-index values appear on top of lower ones."
        },
        {
            "question_text": "What is the default value of the 'position' property?",
            "options": [
                "relative",
                "fixed",
                "absolute",
                "static"
            ],
            "correct_answer": 3,
            "explanation": "Static elements follow the normal document flow and are not affected by top/bottom/left/right properties."
        },
        {
            "question_text": "Which property is used to control the size of the text?",
            "options": [
                "text-size",
                "font-size",
                "font-style",
                "size"
            ],
            "correct_answer": 1,
            "explanation": "font-size is the standard property (e.g., 16px, 1rem)."
        },
        {
            "question_text": "How do you apply a style to all <p> elements inside a <div>?",
            "options": [
                "div p",
                "div + p",
                "div.p",
                "div > p"
            ],
            "correct_answer": 0,
            "explanation": "The descendant selector (space) targets all matching nested elements."
        },
        {
            "question_text": "What does the 'display: none' property do?",
            "options": [
                "Makes the element transparent",
                "Hides the element and removes it from the document layout",
                "Hides the element but keeps its space in the layout",
                "Moves the element to the bottom"
            ],
            "correct_answer": 1,
            "explanation": "Contrast this with 'visibility: hidden', which keeps the layout space."
        },
        {
            "question_text": "Which CSS unit is relative to the font-size of the root element (html)?",
            "options": [
                "em",
                "px",
                "rem",
                "vh"
            ],
            "correct_answer": 2,
            "explanation": "rem stands for 'Root EM'."
        }
    ]
}