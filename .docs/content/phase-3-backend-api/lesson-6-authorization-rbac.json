{
    "title": "Authorization (RBAC)",
    "has_project": false,
    "video_url": "https://www.youtube.com/watch?v=J1qUK8P7940",
    "content": "# 🚦 Authorization: Role-Based Access Control (RBAC)\n\n**Authentication (AuthN)** asks: \"Are you genuinely Mwero?\" (Solved with Passwords and JWTs).\n**Authorization (AuthZ)** asks: \"Now that I know you are Mwero, are you *allowed* to delete this production database?\"\n\nIn complex applications, every user needs different permissions. Administrators can delete posts; Users can only read posts; Moderators can edit posts. Controlling these overlapping permissions is handled by **RBAC (Role-Based Access Control)**.\n\n---\n\n## 1️⃣ Building the RBAC Middleware\n\nBecause we embedded the user's `role` inside the JWT payload in the previous lesson, every single request that passes through the Auth Middleware now comes with `req.user.role`.\n\nWe can build a second, highly specific Middleware shield that stacks *behind* the Auth shield.\n\n```javascript\n// A Higher Order Function that takes a list of allowed roles, \n// and returns a customized Express Middleware!\nconst requireRole = (allowedRolesArray) => {\n    \n    return (req, res, next) => {\n        // 1. Grab the user's role from the JWT (attached by the previous auth middleware)\n        const userRole = req.user.role;\n\n        // 2. Check if their role exists in the allowed list\n        if (allowedRolesArray.includes(userRole)) {\n            next(); // Access granted\n        } else {\n            res.status(403).json({ error: \"Forbidden: You lack the clearance.\" });\n        }\n    };\n};\n```\n\n---\n\n## 2️⃣ Stacking the Shields in Express\n\nNow, we can chain these Middlewares to create impenetrable, highly granular APIs. Express executes Middlewares in the exact array order you define them.\n\n```javascript\nimport express from 'express';\n\nconst app = express();\n\n// 🟢 PUBLIC ROUTE: No shields. Anyone can view blogs.\napp.get('/api/blogs', (req, res) => {\n    res.json(blogs);\n});\n\n// 🟡 PROTECTED ROUTE: Requires a valid JWT, but ANY role is fine (User, Mod, Admin).\napp.post('/api/blogs/comment', requireAuth, (req, res) => {\n    res.send(\"Comment posted by \" + req.user.userId);\n});\n\n// 🟠 MODERATOR ROUTE: Stacking shields. Must be logged in AND must be a Mod/Admin.\n// Array execution: [Verify Token] -> [Verify Role] -> [Execute Route]\napp.delete('/api/blogs/comments/:id', requireAuth, requireRole(['Moderator', 'Admin']), (req, res) => {\n    res.send(\"Troll comment deleted.\");\n});\n\n// 🔴 GOD-LEVEL ROUTE: Only Admins can enter here.\napp.delete('/api/database', requireAuth, requireRole(['Admin']), (req, res) => {\n    res.send(\"Database wiped.\");\n});\n```\n\n---\n\n## 3️⃣ Resource-Level Authorization (Ownership)\n\nRBAC (`requireRole`) determines what *actions* a user can perform globally. However, it fails at **Resource-Level Authorization**.\n\nImagine Bob is a `User`. Users are allowed to edit blog posts.\nBob sends a `PUT` request to `/api/posts/99`. \nBob passes the `requireAuth` shield. Bob passes the `requireRole(['User'])` shield. \nBut wait... *Alice* wrote Post #99! Bob is trying to edit a post he doesn't own!\n\nTo fix this, we must query the Database to check **Ownership** before allowing the action.\n\n```javascript\napp.put('/api/posts/:postId', requireAuth, async (req, res) => {\n    const postId = req.params.postId;\n    // The JWT tells us the attacker is Bob (User #5)\n    const requestingUserId = req.user.userId; \n\n    // 1. Fetch the specific post from the DB\n    const post = await db.posts.findById(postId);\n    if (!post) return res.status(404).send(\"Not Found\");\n\n    // 2. THE OWNERSHIP CHECK\n    // If Bob (5) !== Alice (2), and Bob isn't an Admin, block him!\n    if (post.authorId !== requestingUserId && req.user.role !== 'Admin') {\n        return res.status(403).json({ error: \"You do not own this resource.\" });\n    }\n\n    // 3. Safe to update\n    await db.posts.update(postId, req.body);\n    res.send(\"Post updated.\");\n});\n```\n\n---\n\n## 4️⃣ Attribute-Based Access Control (ABAC) - The Enterprise Level\n\nAs the application scales into the Enterprise level, RBAC stops working. What if a hospital application states:\n*A Doctor can view a Patient's chart ONLY IF they are actively assigned to that patient, AND the current time is between 8 AM and 5 PM, AND the Doctor's security clearance matches the chart's classification level.*\n\nWriting this inside 50 Express routes is impossible to maintain.\n\nEnterprises use **ABAC (Attribute-Based Access Control)** using Policy Engines like **CASL** or Open Policy Agent (OPA). They centralize all math into mathematical \"Policy Blueprints\".\n\n### Example Architecture using CASL:\nInstead of `if (post.authorId !== userId)`, you write pure English logic:\n\n```javascript\n// At the top of your app, you mathematically define what people can do:\nif (user.role === 'Admin') {\n    can('manage', 'all'); // Admin can do anything\n} else {\n    can('read', 'Post'); // Standard users can read all posts\n    can('update', 'Post', { authorId: user.id }); // Standard users can ONLY update their own posts\n}\n\n// Inside the Express Route, it's just one line:\napp.put('/api/posts/:id', (req, res) => {\n    const post = FetchPost(req.params.id);\n\n    // The Policy Engine calculates all the heavy math automatically!\n    if (req.permissions.cannot('update', post)) {\n        return res.status(403).send();\n    }\n});\n```\n\n---\n\n## 💡 Summary Flow\n\n| Check Type | Question | The Express Implementation |\n|---|---|---|\n| **Authentication** | Are you who you say you are? | Password check -> Issue JWT -> `requireAuth` Shield. |\n| **RBAC (Role)** | Are you allowed to hit this URL endpoint? | `requireRole(['Admin'])` Shield. |\n| **Ownership** | Are you allowed to touch this specific row in the database? | Db Fetch -> `if (dbDoc.owner !== jwt.userId) return 403`. |\n",
    "questions": [
        {
            "question_text": "What is the difference between Authentication and Authorization?",
            "options": [
                "They are the same thing",
                "Authentication verifies identity; Authorization verifies permissions",
                "Authentication is for users; Authorization is for servers",
                "Authentication is for public data; Authorization is for private data"
            ],
            "correct_answer": 1,
            "explanation": "Verify identity first (AuthN), then check what that verified identity can access (AuthZ)."
        },
        {
            "question_text": "What does RBAC stand for?",
            "options": [
                "Request Based Access Control",
                "Role-Based Access Control",
                "Random Binary Access Code",
                "Remote Backend Action Center"
            ],
            "correct_answer": 1,
            "explanation": "RBAC assigns permissions to roles (like 'admin') rather than individual users."
        },
        {
            "question_text": "What is the primary benefit of RBAC?",
            "options": [
                "Faster login speed",
                "Easier management; you change access for a whole role instead of 1000 individual users",
                "Better UI design",
                "Users don't need passwords"
            ],
            "correct_answer": 1,
            "explanation": "Efficiency and consistency in scaling permissions."
        },
        {
            "question_text": "What is 'ABAC' (Attribute-Based Access Control)?",
            "options": [
                "A simpler version of RBAC",
                "Permission based on attributes like department, time of day, or location (more granular than roles)",
                "Permission for alphabet apps",
                "Deleting attributes"
            ],
            "correct_answer": 1,
            "explanation": "ABAC allows for very specific logic (e.g., 'Only show this file to Managers in HR during working hours')."
        },
        {
            "question_text": "What is the 'Principle of Least Privilege'?",
            "options": [
                "Giving everyone admin access",
                "Users should only have the minimum access necessary to perform their job",
                "Ignoring security",
                "Charging users for permissions"
            ],
            "correct_answer": 1,
            "explanation": "This reduces the damage if an account is compromised."
        },
        {
            "question_text": "Where do you typically store a user's role?",
            "options": [
                "In the JWT Payload and the Database",
                "In a text file",
                "In the user's browser history",
                "In the CSS"
            ],
            "correct_answer": 0,
            "explanation": "Storing it in the JWT allows for fast checks on the server without a DB lookup for every request."
        },
        {
            "question_text": "How do you protect a route so ONLY admins can access it?",
            "options": [
                "Naming the route /admin",
                "Using a middleware that checks `req.user.role === 'admin'`",
                "Telling the user not to click it",
                "Using a hidden button"
            ],
            "correct_answer": 1,
            "explanation": "Middleware is the standard way to enforce RBAC."
        },
        {
            "question_text": "What is a 'Permission Matrix'?",
            "options": [
                "A movie about code",
                "A table showing which roles have which permissions (Create, Read, Update, Delete)",
                "A complex algorithm",
                "A list of all users"
            ],
            "correct_answer": 1,
            "explanation": "Essential for planning security in large applications."
        },
        {
            "question_text": "What is 'Scope' in OAuth?",
            "options": [
                "A telescope for code",
                "A mechanism to limit an application's access to a user's account (e.g., 'read-only' access)",
                "A new feature in JS",
                "A global variable"
            ],
            "correct_answer": 1,
            "explanation": "Scopes define the 'width' of the access given to a 3rd party app."
        },
        {
            "question_text": "In a blog app, which role should have the 'delete post' permission?",
            "options": [
                "Guest",
                "The Post Author or Admin",
                "Any logged-in user",
                "Nobody"
            ],
            "correct_answer": 1,
            "explanation": "Ownership is a key part of authorization (Resource-Based Authorization)."
        },
        {
            "question_text": "What is 'Multi-Tenancy'?",
            "options": [
                "Having 10 users",
                "A single instance of software serving multiple groups of users (tenants) while keeping their data completely isolated",
                "Renting a server",
                "A type of cloud storage"
            ],
            "correct_answer": 1,
            "explanation": "Crucial for SaaS platforms (like Slack or Trello)."
        },
        {
            "question_text": "Which HTTP status code should you return if a user is logged in but doesn't have the right permissions?",
            "options": [
                "401 Unauthorized",
                "403 Forbidden",
                "404 Not Found",
                "500 Server Error"
            ],
            "correct_answer": 1,
            "explanation": "401 means 'I don't know who you are'; 403 means 'I know you, but you aren't allowed here'."
        },
        {
            "question_text": "What is a 'Super Admin' role?",
            "options": [
                "A role with zero permissions",
                "A special role with absolute power over the entire system and all other admins",
                "A user with a subscription",
                "A myth"
            ],
            "correct_answer": 1,
            "explanation": "Super Admins are usually the developers or platform owners."
        },
        {
            "question_text": "How do you handle 'Dynamic Permissions' (permissions that change based on data)?",
            "options": [
                "You can't do it with RBAC",
                "By writing custom logic inside your middleware or using advanced libraries like CASL",
                "By restarting the server",
                "By asking the user"
            ],
            "correct_answer": 1,
            "explanation": "Dynamic logic handles things like 'Can edit post ONLY IF it was created in the last 2 hours'."
        },
        {
            "question_text": "Why is it dangerous to trust the 'user_id' sent in a POST request body for authorization?",
            "options": [
                "It isn't dangerous",
                "A user could change the ID to someone else's and steal/modify their data",
                "ID is too long",
                "It ruins the formatting"
            ],
            "correct_answer": 1,
            "explanation": "ALWAYS use the user_id extracted from the secure token (JWT) instead of the request body."
        },
        {
            "question_text": "What is 'Revoking' a permission?",
            "options": [
                "Saying it again",
                "Taking away access that was previously given",
                "Adding a new role",
                "Deleting a user"
            ],
            "correct_answer": 1,
            "explanation": "Systems must be able to remove access instantly if a user is fired or a role changes."
        }
    ]
}